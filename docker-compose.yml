version: '2'
services:
  app:
    build:
      context: .
    restart: always
    volumes:
      - ../data:/data
    ports:
      - "8000:8000"
  manage:
    extends:
      service: app
    entrypoint: ["python", "manage.py"]
    volumes:
      - ".:/usr/src/app"
    command: []
